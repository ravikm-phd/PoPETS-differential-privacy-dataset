Think-Aloud Protocol Transcript: Differential Privacy Libraries Usability Study

Participant: P19 (PhD Student, US, 8 Py. Yrs, 5 DS Yrs, Novice, Academia)

---

Ravi Mahankali: Thank you for participating in this study. We are interested in understanding how developers use Differential Privacy libraries in practice. Today you will work on two data analysis tasks using three DP libraries. As you work, please think aloud — narrate your thoughts, questions, and decisions as you would when explaining your process to a colleague. There are no right or wrong answers; we want to observe your natural approach to solving these problems. You may use any resources you normally rely on: documentation, online search, Stack Overflow, or AI coding assistants if that is part of your workflow. Feel free to ask me clarifying questions, but I won't guide your implementation decisions. The session will be audio and screen recorded, and all data will be anonymised. Do you have any questions before we begin?

P19: No, that all makes sense. I am ready to start.

Ravi Mahankali: Great. Before we start the tasks, a few background questions. How many years of Python programming experience do you have, and how many years of data science or analytics experience?

P19: I'm a PhD student at a US university focusing on computational social science. I have about 8 years of Python programming experience and 5 years of data science and statistical modeling experience.

Ravi Mahankali: Thanks. What is your prior experience with Differential Privacy, and how familiar are you with IBM DiffPrivLib, PyDP, or OpenDP specifically?

P19: I consider myself a novice with Differential Privacy. I understand the high-level theoretical concepts—epsilon privacy budgets, adding noise to queries, and basic composition—from reading papers, but I haven't implemented production DP algorithms using specialized Python libraries before today.

Ravi Mahankali: Perfect. What does your typical development environment look like?

P19: VS Code and Jupyter notebooks on macOS, using standard Python libraries like pandas, NumPy, and matplotlib.

Ravi Mahankali: Excellent. Let us move to the tasks. Your first task is to calculate the number of individuals in the dataset earning more than $50,000 per year using Differential Privacy with total budget epsilon equal to 1.0. You are expected to complete this task using all three libraries in the designated order: IBM DiffPrivLib first, then PyDP, then OpenDP. The dataset is loaded as adult.csv in the session directory. Please think aloud as you work.

P19: Sounds good. Let me load `adult.csv` in pandas first and inspect the ground truth count.

P19: The non-private count for `income > 50K` is 7841. Now I'll implement Task A with IBM DiffPrivLib first.

P19: TypeError: `count_nonzero()` got an unexpected keyword argument 'epsilon'. The high-level tool helper doesn't accept epsilon in this signature, so I'll drop down to the `Laplace` mechanism directly.

P19: DiffPrivLib returned 7837. That worked once I initialized the Laplace mechanism manually.

P19: Now moving to PyDP for Task A. Let me check `pydp.algorithms.laplacian.Count`.

P19: PyDP returned 7840. That executed smoothly after converting the column to an integer list.

P19: Now let's try OpenDP for Task A. I'll check the OpenDP documentation for pipeline construction.

P19: I see modules like `transformations`, `measurements`, `combinators`... This looks quite different from what I expected. Instead of calling a simple function, I need to define vector domains, metrics, and chain transformations together.

P19: Exception: `type mismatch: expected i32, found String`. The error comes from OpenDP's Rust core bindings because `make_laplace` expects an integer input domain while my input domain passed strings.

P19: Still getting domain mismatch errors on chaining... I've spent over 10 minutes attempting to construct a working query, and I haven't successfully run a single query. With IBM and PyDP, I at least got results, even if the process was awkward.

P19: Finally got OpenDP to return 7844 after fixing the input domain and type parameter `TIA=int`.

Ravi Mahankali: Great progress. Now let me introduce Task B. Your second task is to calculate the average age of individuals for each unique education level, ensuring Differential Privacy under a total privacy budget of epsilon = 1.0. Please think aloud as you work.

P19: Alright, Task B involves grouped aggregations over 16 distinct education levels.

P19: First, let's look at setting bounds for the `age` column across all three libraries. All three libraries require numeric bounds for aggregation operations, yet none surface a documented, principled procedure for selecting them. It's a catch-22: I need bounds to use the algorithm, but I'm not sure how to set them without looking at the data.

P19: If I plot a histogram of the column before I call this, am I leaking? The warning in DiffPrivLib makes me think yes, but actually it depends on what I do with what I see. If I inspect raw data to pick bounds `(17, 90)`, that empirical selection consumes privacy budget. But setting `(17, 90)` from general domain knowledge about adult census data should be safe.

P19: Now for grouped aggregation: there are 16 unique education levels in `adult.csv`. None of the three libraries provide native DataFrame GroupBy integration.

P19: For privacy budget accounting: if I split total budget `epsilon = 1.0` sequentially across 16 queries, each group gets `1.0 / 16 = 0.0625`. Let me test this in DiffPrivLib.

P19: Small groups like 'Preschool' or 'Doctorate' get wild noise numbers (e.g. 214.7 or -19.3). Splitting epsilon 0.0625 across 16 groups swamps the signal with noise.

P19: If I assume parallel composition holds because education categories form disjoint partitions, passing `epsilon=1.0` per group gives accurate averages ('Bachelors' 38.2, 'Masters' 40.7, 'Doctorate' 44.1). But none of the libraries automatically verify disjointness or enforce parallel composition in code.

Ravi Mahankali: Thank you — that completes the coding tasks. A few closing debrief questions. Which library did you find most usable, and why?

P19: IBM DiffPrivLib felt the most familiar because its API mirrors NumPy and pandas, and its `PrivacyLeakWarning` is a helpful runtime reminder. PyDP was straightforward for single aggregate queries.

Ravi Mahankali: How would you describe your experience with OpenDP?

P19: OpenDP requires you to think about the entire data pipeline. It's very rigorous and mathematically sound, but the learning curve is steep. Spending over 10 minutes trying to set up a basic count query because of Rust type errors and domain chaining was frustrating.

Ravi Mahankali: What was the most challenging aspect of completing these tasks overall?

P19: Two things: the catch-22 of setting bounds without leaking privacy, and the complete lack of native DataFrame GroupBy integration with automatic parallel composition tracking.

Ravi Mahankali: What would you recommend to maintainers of DP libraries?

P19: Provide documented, principled guidance on selecting bounds without leaking privacy, add native DataFrame GroupBy helpers that track parallel composition, and simplify type error tracebacks in OpenDP.

Ravi Mahankali: Thank you very much for your time and feedback.

P19: Thank you!
