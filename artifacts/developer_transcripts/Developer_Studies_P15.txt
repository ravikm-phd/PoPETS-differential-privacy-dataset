Think-Aloud Protocol Transcript: Differential Privacy Libraries Usability Study

Participant: P15 (Assistant Prof., UK, 13 Py. Yrs, 9 DS Yrs, Intermediate, Academia)

---

Ravi Mahankali: Thank you for participating in this study. We are interested in understanding how developers use Differential Privacy libraries in practice. Today you will work on two data analysis tasks using three DP libraries. As you work, please think aloud — narrate your thoughts, questions, and decisions as you would when explaining your process to a colleague. There are no right or wrong answers; we want to observe your natural approach to solving these problems. You may use any resources you normally rely on: documentation, online search, Stack Overflow, or AI coding assistants if that is part of your workflow. Feel free to ask me clarifying questions, but I won't guide your implementation decisions. The session will be audio and screen recorded, and all data will be anonymised. Do you have any questions before we begin?

P15: No, that all makes sense. I am ready to start.

Ravi Mahankali: Great. Before we start the tasks, a few background questions. How many years of Python programming experience do you have, and how many years of data science or analytics experience?

P15: I'm an Assistant Professor at a UK university, working in quantitative social science and privacy-aware machine learning. I have about 13 years of Python experience, going back to my PhD, and about 9 years of data science and statistical modeling experience.

Ravi Mahankali: Thanks. What is your prior experience with Differential Privacy, and how familiar are you with PyDP, OpenDP, or IBM DiffPrivLib specifically?

P15: I have an intermediate theoretical understanding of Differential Privacy. I teach basic privacy concepts in my courses — epsilon privacy budgets, Laplace mechanisms, sensitivity, and sequential versus parallel composition. However, I haven't implemented production DP algorithms using these specialized Python libraries yet, so today is my first hands-on test with PyDP, OpenDP, and DiffPrivLib.

Ravi Mahankali: Perfect. What does your typical development environment look like?

P15: JupyterLab and VS Code on macOS, using standard scientific libraries like pandas, NumPy, and scikit-learn. I rely on official library documentation when evaluating new packages.

Ravi Mahankali: Excellent. Let us move to the tasks. Your first task is to calculate the number of individuals in the dataset earning more than $50,000 per year using Differential Privacy with total budget epsilon equal to 1.0. You are expected to complete this task using all three libraries in the designated order: PyDP first, then OpenDP, then IBM DiffPrivLib. The dataset is already loaded as adult.csv in the session directory. Please think aloud as you work.

P15: Right. Let me start by loading `adult.csv` in pandas.

P15: The dataset has an `income` column with values `<=50K` and `>50K`. The non-private count of rows with `income > 50K` in pandas is 7841. Now I'll implement the DP count starting with PyDP.

P15: PyDP returned 7838. That executed cleanly. The `Count` class accepts epsilon directly and handles noise addition, though I had to pre-filter the data into a list first.

P15: Now moving to OpenDP for Task A. Let me look at the OpenDP documentation and submodule structure.

P15: Looking at the OpenDP module imports... I see modules like 'transformations', 'measurements', 'combinators'... This looks quite different from what I expected. Instead of high-level function calls, it exposes a formal structural framework.

P15: Let me try building a measurement chain in OpenDP.

P15: I hit an error — looking at the traceback: `type mismatch: expected i32, found String`. The error comes from OpenDP's Rust core bindings because the measurement expects an integer carrier type while my input vector passed string data. The type system error messages reference Rust internal types rather than standard Python data structures.

P15: To fix it, I'll convert the filtered data to an integer list before feeding it into the OpenDP pipeline.

P15: That ran and returned 7844. The pipeline works, but navigating the type strictness required extra trial and error.

P15: Now let's try IBM DiffPrivLib for Task A. I'll check `diffprivlib.tools.count_nonzero`.

P15: TypeError: `count_nonzero()` got an unexpected keyword argument 'epsilon'. The function signature in this version doesn't accept epsilon directly, so I'll drop down to the Laplace mechanism.

P15: Got 7836 in DiffPrivLib. That finishes Task A across PyDP, OpenDP, and DiffPrivLib.

Ravi Mahankali: Great progress. Your second task is to calculate the average age of individuals for each unique education level, ensuring Differential Privacy under a total privacy budget of epsilon = 1.0. Please think aloud as you work.

P15: Task B requires calculating a grouped mean across the 16 unique education levels.

P15: First, let's look at setting bounds for numeric attributes like `age`. In DiffPrivLib, calling `diffprivlib.tools.mean` without bounds triggers a `PrivacyLeakWarning: Bounds have not been specified and will be inferred from the data.`

P15: This raises an interesting methodological question about exploratory data analysis: If I plot a histogram of the column before I call this, am I leaking? The warning makes me think yes, but actually it depends on what I do with what I see. If I inspect raw data to pick bounds `(17, 90)`, that empirical bound selection consumes uncounted privacy budget. But if I set `(17, 90)` from general domain knowledge about adult surveys, it's valid.

P15: Now let's try the grouped mean across education levels. None of the three libraries — PyDP, OpenDP, or DiffPrivLib — provide native DataFrame GroupBy integration. So I must write a manual loop over education categories in Python.

P15: For composition: if I split total budget `epsilon = 1.0` sequentially across 16 groups, each group gets `1.0 / 16 = 0.0625`. Let's test this split budget in PyDP.

P15: Look at the results for small groups — 'Preschool' is 214.7 and 'Doctorate' is -19.3! PyDP exhibited the same limitation; I'm stuck in the same place. Without native groupby support that understands parallel composition, I can't get usable results.

P15: If I assume parallel composition holds because education categories form disjoint partitions, passing `epsilon=1.0` per group gives accurate averages ('Bachelors' 38.2, 'Masters' 40.7, 'Doctorate' 44.1). But the library provides no built-in tracking to verify that partitions are disjoint or enforce parallel composition.

Ravi Mahankali: Thank you — that concludes the coding tasks. A few closing debrief questions. Which library did you find most usable, and why?

P15: DiffPrivLib felt familiar because of its NumPy-like API, and its `PrivacyLeakWarning` is a great runtime safety prompt. PyDP was straightforward for single mechanisms, but lacked bounds warnings. OpenDP is theoretically rigorous, but its core pipeline abstractions (`transformations`, `measurements`, `combinators`) and Rust error messages create a high barrier for researchers.

Ravi Mahankali: What was the most challenging aspect of completing these tasks?

P15: The complete absence of native DataFrame GroupBy helpers that automatically handle parallel composition. Writing manual loops forces a choice between swamping small groups with noise under sequential budget splits or making unverified parallel composition assumptions.

Ravi Mahankali: Did you feel confident in your implementations?

P15: For single aggregate counts, yes. For grouped means, no — because handling composition and bounds selection manually in code leaves too much risk of uncounted privacy leakage.

Ravi Mahankali: What would you recommend to library maintainers?

P15: Add native DataFrame GroupBy integration with automatic parallel composition tracking, translate low-level Rust type errors into clear Python messages, and provide clear documentation on handling bounds without leaking privacy during exploratory analysis.

Ravi Mahankali: Thank you very much for your time and feedback.

P15: Thank you!