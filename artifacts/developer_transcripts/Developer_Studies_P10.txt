Think-Aloud Protocol Transcript: Differential Privacy Libraries Usability Study

Participant: P10 (Lead Data Engineer, US, 15 Py. Yrs, 12 DS Yrs, Intermediate, Industry)

---

Ravi Mahankali: Thank you for participating in this study. We are interested in understanding how developers use Differential Privacy libraries in practice. Today you will work on two data analysis tasks using three DP libraries. As you work, please think aloud — narrate your thoughts, questions, and decisions as you would when explaining your process to a colleague. There are no right or wrong answers; we want to observe your natural approach to solving these problems. You may use any resources you normally rely on: documentation, online search, Stack Overflow, or AI coding assistants if that is part of your workflow. Feel free to ask me clarifying questions, but I won't guide your implementation decisions. The session will be audio and screen recorded, and all data will be anonymised. Do you have any questions before we begin?

P10: No, everything makes sense. I'm ready to start.

Ravi Mahankali: Great. Before we start the tasks, a few background questions. Could you summarize your role, Python experience, and data science background?

P10: I work as a Lead Data Engineer at an enterprise technology company in the US. I've been programming in Python for about 15 years, primarily focused on large-scale data infrastructure, ETL pipelines, and feature engineering. For data science and analytics specifically, I've got about 12 years of hands-on experience working with pandas, NumPy, and SQL-based analytics engines.

Ravi Mahankali: Thanks. What is your prior experience with Differential Privacy, and how familiar are you with PyDP, DiffPrivLib, or OpenDP specifically?

P10: I'd describe my DP familiarity as intermediate. I understand the core concepts — epsilon budgets, sensitivity, Laplace and Gaussian mechanisms, and the mathematical difference between sequential and parallel composition. However, my team is currently evaluating these libraries for production deployment, so today is my first time working through concrete implementation pipelines with PyDP, IBM DiffPrivLib, and OpenDP.

Ravi Mahankali: Perfect. What does your typical development environment look like?

P10: VS Code on macOS, Jupyter notebooks for prototyping, and standard Python virtual environments. I rely heavily on official documentation and inspect function signatures directly in Python.

Ravi Mahankali: Excellent. Let us move to the tasks. Your first task is to calculate the number of individuals in the dataset earning more than $50,000 per year using Differential Privacy with total budget epsilon equal to 1.0. You are expected to complete this task using all three libraries in the designated order: PyDP first, then IBM DiffPrivLib, then OpenDP. The dataset is already loaded as adult.csv in the session directory. Please think aloud as you work.

P10: Got it. Let me first inspect `adult.csv` using pandas to see how the columns are structured.

P10: Looking at `adult.csv`, I see columns like `age`, `workclass`, `education`, `marital-status`, `occupation`, and `income`. The `income` column has string values `<=50K` and `>50K`. The exact count of rows where income is `>50K` in raw pandas is `len(df[df['income'].str.strip() == '>50K'])`. That gives 7841. Now let's calculate the differentially private count using PyDP first.

P10: Starting Task A with PyDP. Let me pull up the PyDP documentation to check the `Count` class interface.

P10: PyDP exposes a `Count` algorithm from Google's C++ DP library. It takes `epsilon` and `dtype` in the constructor. To set bounding limits or parameters for data, mechanisms often require min and max values. But here's an interesting privacy observation: if I compute min/max from the data and pass them in, PyDP has no way of knowing that I 'cheated'! The library doesn't track whether bounds were derived privately or from raw data inspection. That's a potential uncounted privacy leak.

P10: For a simple count, sensitivity is 1. Let's pass the filtered binary indicators or income values to PyDP's `Count`.

P10: PyDP returned 7838. That executed cleanly. The interface accepted epsilon directly in the constructor and gave a DP count without needing manual noise addition, though I had to convert the pandas Series into a Python list first.

P10: Now let's test IBM DiffPrivLib for Task A. I'll search `diffprivlib.tools` for a count function.

P10: Let's see what GitHub Copilot suggests for IBM DiffPrivLib counting.

P10: Copilot auto-completed `result = count_nonzero(mask, epsilon=1.0)`. That looks like standard NumPy/DiffPrivLib syntax. Let me run Copilot's suggested line.

P10: I hit a TypeError: `count_nonzero() got an unexpected keyword argument 'epsilon'`. Copilot hallucinated that `epsilon` is an argument for `count_nonzero`! Copilot assumed that because it's a DP library, all tools functions accept `epsilon=1.0`. But the actual function signature doesn't take `epsilon` keyword argument in this version. The AI auto-complete led me straight into a runtime exception. So I'll fall back to the mechanism layer.

P10: DiffPrivLib returned 7836. Operates well at the mechanism level, but required manual true-count computation in pandas first.

P10: Now let's try OpenDP for Task A. OpenDP uses measurement pipelines.

P10: OpenDP returned 7844. It required setting up explicit domain and metric types, but the measurement pipeline executed cleanly once configured.

Ravi Mahankali: Great progress. Your second task is to calculate the average age of individuals for each unique education level, ensuring Differential Privacy under a total privacy budget of epsilon = 1.0. Please think aloud as you work.

P10: Task B is grouped aggregation across education levels. Grouping is central to production data engineering pipelines. In `adult.csv`, there are 16 unique education categories.

P10: Let's start Task B with PyDP. I'll search for native GroupBy support in PyDP.

P10: PyDP does not have native DataFrame GroupBy support. So I have to write a manual Python loop over the unique education categories.

P10: Now, let's address budget composition. To maintain a total privacy budget of epsilon = 1.0 across 16 queries under sequential composition, I must split the budget so that each group query receives `epsilon = 1.0 / 16 = 0.0625`. Let's test this split budget in PyDP with `BoundedMean`.

P10: Look at these output numbers. For 'Preschool' the average age is 214.7, and for 'Doctorate' it is -19.3! With such low epsilon per group, the noise completely overwhelms the signal... the library doesn't provide any guidance on how to balance this tradeoff or warn users when utility will be severely impacted.

P10: If I assume parallel composition holds because the 16 education categories form disjoint partitions of the population, I could assign `epsilon = 1.0` to each group.

P10: With epsilon = 1.0 per group, the results are accurate: 'Bachelors' 38.2, 'Masters' 40.7, 'Doctorate' 44.1. But PyDP has no mechanism to declare parallel composition or verify that groups are disjoint. The user is left entirely responsible for proving that parallel composition applies.

Ravi Mahankali: Please move to IBM DiffPrivLib for Task 2.

P10: DiffPrivLib has a `diffprivlib.tools.mean` function. Let me test calling it without explicit bounds first to see what happens.

P10: It ran, but look at the warning: `PrivacyLeakWarning: Bounds have not been specified and will be inferred from the data. This will result in additional privacy leakage.` I really appreciate this warning! It explicitly tells me that inferring bounds from data violates privacy guarantees. So I will supply explicit bounds `(17, 90)`.

P10: Now for the grouped version in DiffPrivLib. Like PyDP, DiffPrivLib lacks native GroupBy support. Writing the manual loop produces the same composition dilemma: splitting epsilon to 0.0625 yields unusable noise, while using 1.0 per group relies on unverified parallel composition.

Ravi Mahankali: Finally, OpenDP for Task 2.

P10: OpenDP requires building a measurement pipeline for floating-point vectors, clamping values to bounds `(17, 90)`, and computing the Laplace mean.

P10: OpenDP executed for individual groups with a mean near 38.1 for Bachelors. It offers strong mathematical guarantees, but constructing pipelines inside manual loops for grouped queries imposes significant cognitive overhead on data engineers.

Ravi Mahankali: Thank you — that completes the coding tasks. I have four closing debrief questions. First, which library did you find most usable for these tasks, and why?

P10: IBM DiffPrivLib felt the most natural for a Python data engineer because its API mirrors NumPy and pandas conventions, and its `PrivacyLeakWarning` is a great safety feature. PyDP had a clean `Count` class for single queries, but lacked bound safety checks. OpenDP is mathematically rigorous, but its low-level pipeline API has a steep learning curve for routine data analysis.

Ravi Mahankali: Second, what was the most challenging aspect of completing these tasks?

P10: The complete lack of native DataFrame GroupBy support across libraries, combined with the composition and utility trade-offs. Having to manually write loops and divide epsilon budget led to massive noise that overwhelmed small groups, and no library warned me when utility was destroyed or helped enforce parallel composition.

Ravi Mahankali: Third, did you feel confident that your implementations were correct and privacy-preserving?

P10: For single count queries, yes. For grouped aggregations, less so — because deriving bounds or assuming parallel composition requires manual proofs outside the library, with no system guardrails to prevent uncounted privacy leaks.

Ravi Mahankali: Fourth, what recommendations would you give to maintainers of DP libraries?

P10: First, build native DataFrame GroupBy helpers that automatically encode parallel composition for disjoint partitions. Second, provide active runtime feedback and guidance when chosen epsilon budgets or bounds severely degrade utility. Third, catch uncounted privacy leaks when users supply empirical bounds without budget accounting.

Ravi Mahankali: Thank you very much for your time and feedback.

P10: Thank you! This was a very insightful and educational study.